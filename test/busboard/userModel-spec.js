
describe( 'userModel', function( ) {

    var broker = require( '../../mongoBroker' );
    var user = { 'user' : 'testuser', 'pass' : 'testpass' };

    it( 'Should check if username is in use before adding', function( ) {} );

        
});
